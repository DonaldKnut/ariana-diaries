import React from "react";
import ForgotPassword from "../_components/ForgotPassword";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ForgotPassword />
    </div>
  );
};

export default page;
