import React from "react";
import ResetPassword from "../_components/ResetPassword";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ResetPassword />
    </div>
  );
};

export default page;
