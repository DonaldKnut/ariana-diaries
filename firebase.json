{
  "storage": {
    "rules": "firebase-storage.rules",
    "cors": [
      {
        "origin": ["http://localhost:3000"],
        "method": ["GET", "PUT", "POST", "DELETE"],
        "maxAgeSeconds": 3600
      }
    ]
  }
}
